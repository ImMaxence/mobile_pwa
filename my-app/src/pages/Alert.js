import React from 'react';
import Layout from '../components/Layout';

const Alert = () => {
    return (
        <Layout>
            <div>
                alert
            </div>
        </Layout>
    );
};

export default Alert;