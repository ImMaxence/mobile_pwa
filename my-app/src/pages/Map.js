import React from 'react';
import Layout from '../components/Layout';

const Map = () => {
    return (
        <Layout>
            <div>
                map
            </div>
        </Layout>
    );
};

export default Map;