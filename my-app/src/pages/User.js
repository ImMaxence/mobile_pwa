import React from 'react';
import VersionApp from '../components/VersionApp';

const User = () => {
    return (
        <div>
            <VersionApp />
        </div>
    );
};

export default User;